name: React CI
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

concurrency:
  group: ${{ github.workflow }}-starter-blog-ui-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./starter/blog-ui
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: npm
          cache-dependency-path: ./starter/blog-ui/package-lock.json

      - name: Install dependencies
        run: if [ -f package-lock.json ]; then npm ci || npm install; else npm install; fi

      - name: Build
        run: npm run build

      - name: Test
        # Remove 'continue-on-error' when your tests are stable
        run: npm test --if-present -- --watchAll=false --passWithNoTests
        continue-on-error: true
